providers = ["node", "python"]

[phases.setup]
nixPkgs = ["nodejs", "python3"]

[phases.install]
cmds = [
  "npm install",
  "pip install -r requirements.txt"
]

[phases.build]
cmds = ["npm run postinstall"]

[start]
cmd = "node scripts/run-with-prisma.js server"
